// 화살표 함수
const add = (x, y) => x + y;
console.log(add(2, 5)); // 7

// 함수 선언문
function sum(x, y) {
  return x + y;
}

// 함수 호출
// 인수 1과 2가 매개분수 x와 y에 순서대로 할당되고
// 함수 몸체의 문들이 실행된다.
const result = sum(1, 2);
