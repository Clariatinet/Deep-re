// 10 + 20은 평가되어 숫자 값 30을 생성한다.
10 + 20; // 30

// 변수에는 10 + 20이 평가되어 생성된 숫자 값 30이 할당된다.
const sum = 10 + 20;

// 숫자 리터럴 3
3;

const score = 100;
const value = 50 + 50;

console.log(score);

// 리터럴 표현식
10;
('hello');

// 식별자 표현식 (선언이 이미 존재한다고 가정)
sum;
person.name;
arr[1];

// 연산자 표현식
10 + 20;
sum = 10;
sum !== 10;
